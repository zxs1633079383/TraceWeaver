# TraceWeaver Phase 6 — 执行计划

> 执行方法：TDD + Subagent 驱动。每个 Task 独立，先写测试（RED），再实现（GREEN），代码审查后提交。

---

## Task 概览

| ID | 任务 | 状态 | 依赖 |
|----|------|------|------|
| T01 | FeedbackLog — 经验持久化 | pending | - |
| T02 | HarnessValidator — 对齐检测 | pending | T01 |
| T03 | TriggerExecutor 接入 FeedbackLog | pending | T01 |
| T04 | OTel 多适配器 + OTLP/gRPC | pending | - |
| T05 | Daemon 初始化：FeedbackLog + Validator + FsWatcher harness 监听 | pending | T01,T02,T03,T04 |
| T06 | IPC 新方法：feedback_query / feedback_summary / harness_validate | pending | T01,T02 |
| T07 | CLI：tw feedback + tw harness validate | pending | T06 |
| T08 | 验收：全测试 + Jaeger 冒烟 + Example 更新 | pending | ALL |

---

## Task 详情

### T01 — FeedbackLog

**文件：**
- 创建：`packages/tw-daemon/src/feedback/feedback-log.ts`
- 创建：`packages/tw-daemon/src/feedback/feedback-log.test.ts`

**接口：**
```typescript
class FeedbackLog {
  load(): void
  record(input: FeedbackRecordInput): FeedbackEntry
  query(params: FeedbackQuery): FeedbackEntry[]
  getSummary(harness_id: string): HarnessFeedbackSummary
  getAllSummaries(): HarnessFeedbackSummary[]
}
```

**测试覆盖：**
- record + getHistory 返回正确条目
- seq 自增
- query 按 harness_id / result / limit 过滤
- 跨实例重放（NDJSON 持久化）
- getSummary：total/pass/fail/failure_rate
- getSummary：consecutive_failures 计算
- getSummary：trend 算法（improving/degrading/stable）
- getAllSummaries 按 harness_id 分组

---

### T02 — HarnessValidator

**文件：**
- 创建：`packages/tw-daemon/src/harness/validator.ts`
- 创建：`packages/tw-daemon/src/harness/validator.test.ts`

**接口：**
```typescript
class HarnessValidator {
  constructor(loader: HarnessLoader, feedbackLog: FeedbackLog, opts?: { consecutiveFailThreshold?: number })
  validate(entities: Entity[]): AlignmentIssue[]
}
```

**测试覆盖：**
- 无问题时返回空数组
- 孤儿引用检测（severity=error）
- 死约束检测（severity=warning）
- 持续失败检测（threshold 可配置）
- issue.suggestion 字段非空
- 低于阈值时不报持续失败

---

### T03 — TriggerExecutor 接入 FeedbackLog

**文件：**
- 修改：`packages/tw-daemon/src/trigger/executor.ts`

**改动：**
1. `TriggerExecutorOptions` 新增 `feedbackLog?: FeedbackLog`
2. `evaluateAndAct()` 评估后调用 `feedbackLog?.record(...)`
3. `consecutive_failures >= CONSECUTIVE_FAIL_ALERT(3)` 时写入收件箱建议
4. 计时 `duration_ms`

**测试：** 在 `executor.test.ts` 中新增：
- 评估后 feedbackLog 有记录
- 连续失败 3 次后收件箱有 FEEDBACK 消息

---

### T04 — OTel 多适配器 + OTLP/gRPC

**文件：**
- 修改：`packages/tw-daemon/src/otel/exporter.ts` → 重命名/包装为 OtlpHttpExporter
- 创建：`packages/tw-daemon/src/otel/exporter-grpc.ts`
- 创建：`packages/tw-daemon/src/otel/exporter-registry.ts`
- 创建：`packages/tw-daemon/src/otel/proto/` (OTLP proto 文件)
- 依赖：新增 `@grpc/grpc-js`, `@grpc/proto-loader`

**gRPC 实现关键点：**
```typescript
// 使用 @grpc/grpc-js 动态加载 OTLP proto
const packageDef = protoLoader.loadSync(PROTO_PATH, { keepCase: true, longs: String, enums: String })
const TraceServiceProto = grpc.loadPackageDefinition(packageDef)
const client = new TraceServiceProto.TraceService(endpoint, grpc.credentials.createInsecure())
```

**测试：** mock gRPC client，验证 proto payload 格式正确

---

### T05 — Daemon 初始化

**文件：**
- 修改：`packages/tw-daemon/src/index.ts`

**改动：**
1. 初始化 `FeedbackLog(join(STORE_DIR, 'feedback.ndjson'))`
2. 初始化 `HarnessValidator(harnessLoader, feedbackLog)`
3. Daemon 启动后首次 validate → 发现问题写 inbox
4. FsWatcher 监听 `join(STORE_DIR, 'harness')` 变更 → rescan + revalidate
5. `TriggerExecutor` 注入 `feedbackLog`
6. `ExporterRegistry` 根据 `TW_OTEL_EXPORTER` 环境变量注册适配器
7. Cleanup 中 `await exporterRegistry.shutdown()`

---

### T06 — IPC 新方法

**文件：**
- 修改：`packages/tw-daemon/src/ipc-server.ts`

**新增 dispatch cases：**
```typescript
'feedback_query'   → feedbackLog.query(params)
'feedback_summary' → params.harness_id ? [feedbackLog.getSummary(id)] : feedbackLog.getAllSummaries()
'harness_validate' → validator.validate(handler.getAll())
```

---

### T07 — CLI 新命令

**文件：**
- 创建：`packages/tw-cli/src/commands/feedback.ts`
- 修改：`packages/tw-cli/src/commands/harness.ts`（新增 `validate` 子命令）
- 修改：`packages/tw-cli/src/index.ts`

---

### T08 — 验收

1. 运行全量测试：目标 205+ tests passing
2. `npm run build` 无报错
3. 启动 Daemon → 运行 `tw feedback summary` + `tw harness validate`
4. 配置 `TW_OTEL_EXPORTER=otlp-grpc TW_OTEL_ENDPOINT=http://jaeger:4317` 后导出 span
5. Jaeger UI 验证链路可见
6. 更新 Example 11 加入 FeedbackLog + Validator 演示段落

---

## 验收标准汇总

```
✅ feedback.ndjson 在评估后生成
✅ tw feedback summary 显示 harness 评估统计
✅ tw harness validate 报告对齐问题
✅ 连续失败 3 次 → 收件箱有 [FEEDBACK] 建议消息
✅ harness 文件变更 → 自动 rescan + revalidate
✅ OTLP/gRPC 适配器可配置启用
✅ Jaeger UI 中 service=traceweaver-daemon 可查到 span
✅ npm test: 205+ passing
✅ npm run build: 0 errors
```
